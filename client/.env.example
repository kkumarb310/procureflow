# Base URL of the ProcureFlow API.
# Leave blank in development to use the Vite proxy (/api -> localhost:4000).
# In production (Vercel) set this to your deployed API, e.g. https://procureflow-api.onrender.com
VITE_API_URL=
